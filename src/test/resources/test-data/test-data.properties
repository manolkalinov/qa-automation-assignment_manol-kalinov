# Test data properties — reserved for externalized test inputs.
# Currently test data is hardcoded in test classes.
# Intended to supply values such as known resource IDs, boundary values,
# and payload templates to tests via a dedicated data-loading mechanism.
